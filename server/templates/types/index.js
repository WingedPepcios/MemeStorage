const user = require('./userTypes');
const regex = require('./regexTypes');
const memeTypes = require('./memeTypes');
const pages = require('./pagesTypes');
const labels = require('./tagTypes');

module.exports = {
  user,
  regex,
  memeTypes,
  pages,
  labels,
};
